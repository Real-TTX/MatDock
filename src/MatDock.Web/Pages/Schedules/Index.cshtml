@page
@model MatDock.Web.Pages.Schedules.IndexModel
@{
    ViewData["Title"] = "Schedules";
}

<partial name="_BackupTabs" />

<div class="page-head">
    <div class="page-head__title">
        <h1>Backup schedules</h1>
        <span class="page-head__subtitle">Automatic backups with retention</span>
    </div>
</div>

@if (!string.IsNullOrEmpty(Model.StatusMessage))
{
    <div class="alert @(Model.IsError ? "alert--error" : "alert--success")">
        <icon name="@(Model.IsError ? "x-circle" : "check-circle")" /><span>@Model.StatusMessage</span>
    </div>
}

<mat-table>
    <mat-table-body>
        @if (Model.Items.Count == 0)
        {
            <div class="empty-state"><div><icon name="clock" /></div><p>No schedules yet. Create one.</p></div>
        }
        else
        {
            <table class="data">
                <thead>
                    <tr><th>Name</th><th>Environment</th><th>Volumes</th><th>Schedule (UTC)</th><th>Retention</th><th>Status</th><th>Next run</th><th class="actions">Actions</th></tr>
                </thead>
                <tbody>
                    @foreach (var s in Model.Items)
                    {
                        <tr>
                            <td>
                                <a href="/Schedules/Edit?id=@s.Id"><strong>@s.Name</strong></a>
                                @if (!s.Enabled)
                                {
                                    <span class="mat-badge">disabled</span>
                                }
                            </td>
                            <td>@(Model.EnvironmentNames.TryGetValue(s.EnvironmentId, out var n) ? n : $"#{s.EnvironmentId}")</td>
                            <td>@s.Volumes.Count()</td>
                            <td class="mono">@s.Cron</td>
                            <td class="cell-sub">
                                @(s.RetentionCount > 0 ? $"max {s.RetentionCount}" : "∞")@(s.RetentionDays > 0 ? $", {s.RetentionDays}d" : "")
                            </td>
                            <td class="cell-sub">@(s.LastStatus ?? "–")</td>
                            <td class="text-muted nowrap">@(s.Enabled ? (s.NextRunAt?.ToLocalTime().ToString("dd.MM. HH:mm") ?? "–") : "–")</td>
                            <td class="actions">
                                <div class="row-actions">
                                    <form method="post" asp-page-handler="Run" data-confirm="Run schedule '@s.Name' now?" style="display:inline;">
                                        <input type="hidden" name="id" value="@s.Id" />
                                        <button type="submit" class="mat-btn mat-btn--sm" title="Run now"><icon name="refresh" /></button>
                                    </form>
                                    <a class="mat-btn mat-btn--sm" href="/Schedules/Edit?id=@s.Id" title="Edit"><icon name="edit" /></a>
                                    <span style="width:8px;display:inline-block;"></span>
                                    <form method="post" asp-page-handler="Delete" data-confirm="Delete schedule '@s.Name'?" style="display:inline;">
                                        <input type="hidden" name="id" value="@s.Id" />
                                        <button type="submit" class="mat-btn mat-btn--sm mat-btn--danger" title="Delete"><icon name="trash" /></button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </mat-table-body>

    <mat-table-actions>
        <a href="/Schedules/Edit" class="mat-btn mat-btn--primary"><icon name="plus" /><span>New schedule</span></a>
    </mat-table-actions>
</mat-table>
