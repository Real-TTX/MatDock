@page
@model MatDock.Web.Pages.Apps.IndexModel
@{
    ViewData["Title"] = "Templates";
}

<div class="page-head">
    <div class="page-head__title">
        <h1>Templates</h1>
        <span class="page-head__subtitle">Create &amp; install app templates (Compose blueprints)</span>
    </div>
    <div class="mat-buttons">
        <a href="/Apps/Edit" class="mat-btn mat-btn--primary"><icon name="plus" /><span>New template</span></a>
    </div>
</div>

@if (!string.IsNullOrEmpty(Model.StatusMessage))
{
    <div class="alert @(Model.IsError ? "alert--error" : "alert--success")">
        <icon name="@(Model.IsError ? "x-circle" : "check-circle")" /><span>@Model.StatusMessage</span>
    </div>
}

<div class="card" style="padding:0;">
    <table class="data">
        <thead>
            <tr><th>Name</th><th>Category</th><th>Description</th><th class="actions">Actions</th></tr>
        </thead>
        <tbody>
            @if (Model.Templates.Count == 0)
            {
                <tr><td colspan="4"><div class="empty-state"><div><icon name="template" /></div><p>No templates yet. Create one to install apps as a stack with a single click.</p></div></td></tr>
            }
            @foreach (var t in Model.Templates)
            {
                <tr>
                    <td><a href="/Apps/Edit?id=@t.Id"><strong>@t.Name</strong></a></td>
                    <td class="text-muted">@(string.IsNullOrEmpty(t.Category) ? "–" : t.Category)</td>
                    <td class="cell-sub">@(string.IsNullOrEmpty(t.Description) ? "–" : t.Description)</td>
                    <td class="actions">
                        <div class="row-actions">
                            <a class="mat-btn mat-btn--sm mat-btn--primary" href="/Stacks/Edit?templateId=@t.Id" title="Install"><icon name="download" /><span>Install</span></a>
                            <a class="mat-btn mat-btn--sm" href="/Apps/Edit?id=@t.Id" title="Edit"><icon name="edit" /></a>
                            <span style="width:8px;display:inline-block;"></span>
                            <form method="post" asp-page-handler="Delete" data-confirm="Delete template '@t.Name'?" style="display:inline;">
                                <input type="hidden" name="id" value="@t.Id" />
                                <button type="submit" class="mat-btn mat-btn--sm mat-btn--danger" title="Delete"><icon name="trash" /></button>
                            </form>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
