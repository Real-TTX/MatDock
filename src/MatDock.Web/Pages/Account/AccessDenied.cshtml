@page
@{
    ViewData["Title"] = "Access denied";
    Layout = User.Identity?.IsAuthenticated == true ? "_Layout" : "_Layout";
}

<div class="card" style="max-width:520px;margin:40px auto;">
    <div class="card__body" style="text-align:center;">
        <div style="font-size:2.4rem;color:var(--danger);"><icon name="lock" /></div>
        <h1>Access denied</h1>
        <p class="text-muted">You do not have the required permissions to view this page.</p>
        <a href="/" class="mat-btn mat-btn--primary"><icon name="dashboard" /><span>Go to dashboard</span></a>
    </div>
</div>
