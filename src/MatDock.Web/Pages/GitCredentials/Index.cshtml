@page
@model MatDock.Web.Pages.GitCredentials.IndexModel
@{
    ViewData["Title"] = "Git credentials";
}

<partial name="_SystemTabs" />

<div class="page-head">
    <div class="page-head__title">
        <h1>Git credentials</h1>
        <span class="page-head__subtitle">Credentials for cloning private stack repositories</span>
    </div>
    <div class="mat-buttons">
        <a href="/GitCredentials/Edit" class="mat-btn mat-btn--primary"><icon name="plus" /><span>New git credential</span></a>
    </div>
</div>

@if (!string.IsNullOrEmpty(Model.StatusMessage))
{
    <div class="alert @(Model.IsError ? "alert--error" : "alert--success")">
        <icon name="@(Model.IsError ? "x-circle" : "check-circle")" /><span>@Model.StatusMessage</span>
    </div>
}

<div class="card" style="padding:0;">
    <table class="data">
        <thead>
            <tr><th>Name</th><th>Type</th><th>User</th><th class="actions">Actions</th></tr>
        </thead>
        <tbody>
            @if (Model.Credentials.Count == 0)
            {
                <tr><td colspan="4"><div class="empty-state"><div><icon name="key" /></div><p>No git credentials yet. Create one to clone private repositories.</p></div></td></tr>
            }
            @foreach (var c in Model.Credentials)
            {
                <tr>
                    <td><a href="/GitCredentials/Edit?id=@c.Id"><strong>@c.Name</strong></a></td>
                    <td class="text-muted">@(c.AuthType == MatDock.Core.Entities.GitAuthType.HttpsToken ? "HTTPS-Token" : c.AuthType.ToString())</td>
                    <td class="text-muted">@(string.IsNullOrEmpty(c.Username) ? "–" : c.Username)</td>
                    <td class="actions">
                        <div class="row-actions">
                            <form method="post" asp-page-handler="Delete" data-confirm="Delete git credential @c.Name?" style="display:inline;">
                                <input type="hidden" name="id" value="@c.Id" />
                                <button type="submit" class="mat-btn mat-btn--sm mat-btn--danger" title="Delete"><icon name="trash" /></button>
                            </form>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
