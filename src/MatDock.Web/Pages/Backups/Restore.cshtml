@page
@using MatDock.Core.Volumes
@model MatDock.Web.Pages.Backups.RestoreModel
@{
    ViewData["Title"] = "Restore";
}

<div class="page-head">
    <div class="page-head__title">
        <h1>Restore</h1>
        <span class="page-head__subtitle">Restore a backup into a volume</span>
    </div>
    <div class="mat-buttons">
        <a href="/Backups" class="mat-btn mat-btn--ghost"><icon name="back" /><span>Back</span></a>
    </div>
</div>

@if (Model.Result is not null)
{
    <div class="result-banner @(Model.Result.Success ? "result-banner--ok" : "result-banner--fail")">
        <icon name="@(Model.Result.Success ? "check-circle" : "x-circle")" />
        <div>
            <div class="result-banner__big">@(Model.Result.Success ? "Restore successful" : "Restore failed")</div>
            <div class="result-banner__sub">@Model.Result.Message</div>
        </div>
    </div>
}

@if (Model.Backup is null)
{
    <div class="alert alert--error"><icon name="x-circle" /><span>Backup not found.</span></div>
}
else if (Model.Environments.Count == 0)
{
    <div class="alert alert--warning"><icon name="alert" /><span>No target environment available.</span></div>
}
else
{
    <form method="post" data-confirm="Start restore now?">
        <input type="hidden" asp-for="Input.BackupId" />
        <div asp-validation-summary="ModelOnly"></div>

        <div class="migrate">
            <div class="migrate__side migrate__side--source">
                <h3><icon name="save" /><span>Backup</span></h3>
                <div class="migrate__env">@Model.Backup.VolumeName</div>
                <div class="migrate__host">from @Model.Backup.SourceEnvironmentName · @VolumeMigrationResult.FormatBytes(Model.Backup.SizeBytes) · @Model.Backup.CreateDate.ToLocalTime().ToString("dd.MM.yyyy HH:mm")</div>
            </div>

            <div class="migrate__arrow"><icon name="chevron-right" /></div>

            <div class="migrate__side migrate__side--target">
                <h3><icon name="server" /><span>Target</span></h3>
                <div class="mat-form">
                    <mat-field label="Target environment" asp-for="Input.TargetEnvId">
                        <select asp-for="Input.TargetEnvId" class="mat-select">
                            @foreach (var env in Model.Environments)
                            {
                                <option value="@env.Id">@env.Name — @env.Host</option>
                            }
                        </select>
                    </mat-field>
                    <mat-field label="Target volume" asp-for="Input.TargetVolume">
                        <input asp-for="Input.TargetVolume" class="mat-input" />
                    </mat-field>
                    <label class="mat-check">
                        <input asp-for="Input.Overwrite" type="checkbox" />
                        <span>Overwrite if the target volume already contains data</span>
                    </label>
                    <label class="mat-check">
                        <input asp-for="Input.StopContainers" type="checkbox" />
                        <span>Stop containers using the target volume during the restore, then restart them</span>
                    </label>
                </div>
            </div>
        </div>

        <div class="alert alert--info">
            <icon name="alert" />
            <span>The backup is restored into the target volume via <code>tar</code> over SSH. Requirement: Docker + <code>busybox</code> on the target host.</span>
        </div>

        <mat-form-buttons>
            <button type="submit" class="mat-btn mat-btn--primary"><icon name="refresh" /><span>Start restore</span></button>
            <a href="/Backups" class="mat-btn mat-btn--ghost"><icon name="back" /><span>Cancel</span></a>
        </mat-form-buttons>
    </form>
}
